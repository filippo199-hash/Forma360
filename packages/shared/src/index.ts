// @forma360/shared — barrel file intentionally kept empty.
// Export individual modules (e.g. `@forma360/shared/env`) via the package.json
// `exports` map as they're added in later PRs. See ground rule #9.
export {};

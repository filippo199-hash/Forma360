{
  "name": "@forma360/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "dev": "echo 'next dev — wired in PR 7'",
    "build": "echo 'next build — wired in PR 7'",
    "start": "echo 'next start — wired in PR 7'"
  }
}
